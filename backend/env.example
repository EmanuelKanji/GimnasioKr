# ===========================================
# CONFIGURACIÓN DE PRODUCCIÓN - GIMNASIO API
# ===========================================

# Entorno
NODE_ENV=production

# Puerto del servidor
PORT=4000

# Base de datos MongoDB
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/gimnasio?retryWrites=true&w=majority

# JWT Secret (GENERAR UNO NUEVO Y SEGURO)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui_minimo_32_caracteres
JWT_EXPIRES_IN=24h

# CORS Origins (separados por coma)
CORS_ORIGIN=https://kraccess.netlify.app,https://gimnasiokr.onrender.com

# Logging
LOG_LEVEL=info

# Rate Limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=50

# ===========================================
# INSTRUCCIONES DE CONFIGURACIÓN
# ===========================================
# 1. Copia este archivo como .env
# 2. Cambia todos los valores por los reales
# 3. Genera un JWT_SECRET seguro (openssl rand -base64 32)
# 4. Configura tu MongoDB Atlas o local
# 5. Actualiza CORS_ORIGIN con tus dominios
# 6. NUNCA subas el archivo .env al repositorio
